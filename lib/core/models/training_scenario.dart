class TrainingScenario {
  final String title;
  final String description;
  final String initialPrompt; // The AI's first message

  TrainingScenario({
    required this.title,
    required this.description,
    required this.initialPrompt,
  });
}
